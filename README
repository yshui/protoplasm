#Protoplasm

A simple compiler developed for a Compiler course. The language frontend is
pretty simple, but the IR is feature complete.

This compiler uses a SSA IR, with the exception of memory access.
